<Window x:Class="MathSimulator.RandomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MathSimulator"
        mc:Ignorable="d"
        Title="Метод Монте-Карло" Height="450" Width="800" MinHeight="450" MinWidth="800"
        Icon="/data/static/img/window_icon.ico"
        WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="223*"/>
            <ColumnDefinition Width="177*"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="randomGrid" Background="LightGray" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="40">
                </RowDefinition>
                <RowDefinition>
                </RowDefinition>
            </Grid.RowDefinitions>
            <Canvas Grid.Row="1" Background="WhiteSmoke">
            </Canvas>
        </Grid>
    </Grid>
</Window>
